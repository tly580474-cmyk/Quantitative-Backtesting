# 2026-09-11 服务器数据更新修复

目标：`192.168.2.218`，应用目录 `/opt/quant-backtest`。

## 原因与修复

- 总开关 `BACKGROUND_JOBS_ENABLED=true` 和 systemd 更新 timer 已开启，但 `MARKET_DATA_ENABLED=false`、`MARKET_INDEX_AUTO_UPDATE_ENABLED=false` 仍保留迁移时的禁用配置，日线与指数调度没有启动。
- 交易日历缺少 2026-09-11，且日线未更新，Linux 后续任务的交易日检查返回 `calendar-missing`，任务以成功退出状态跳过实际采集。
- 备份服务器 `.env` 后开启上述两个行情开关，补采 9 月 10—11 日 SH 交易日历和当日日线，重启 `quant-backend` 加载配置。
- `linuxJob.mjs` 增加交易日判断日志，明确记录跳过原因。服务器原脚本备份为 `server/scripts/linuxJob.mjs.before-20260911`。

## 已验证

- 服务器 Linux 调度回归测试：5 项通过。
- 后端启动日志确认：证券主表 15:20、日线 15:30、国内指数 20:00 自动调度开启；分钟、资金流、研究快照 timer 均已 enabled。
- 手动日线任务 `710347d8-b428-40a8-8303-f14dcacea29c` 完成，5,560 个标的，失败 0。
- 重启后自动证券主表任务完成；自动日线任务 `949bda77-ce96-4e67-92bd-de37361bb717` 完成，5,562 个标的，失败 0。
- 9 月 11 日最终日线实际入库 5,550 条；国内指数 10 项更新成功；资金流入库 5,205 条（来源报告覆盖率 88.19%，不是全覆盖）。
- 研究快照 `7ee99aa8-6aa2-4ef3-99c0-c7b5dd756c4c-20260911122706` 已发布并通过校验，最大日期 2026-09-11，日线总行数 17,314,072。
- 分钟数据在证券主表更新后再次补齐：2026-09-11 最终发布 5,550 个标的、1,332,000 行，目标覆盖率 100%，请求错误 0；运行号 `962bc415ecc64316b48bf88550e6a2a1`，20:35:16 完成。原分钟文件和清单保留 `before-universe-repair` 备份。
- 分钟更新器仍报告 610 个标的未完成日线交叉验证（`unverifiedDailyChecks`）；覆盖率与时间轴完整不等于全部通过日线交叉核验，保留该质量限制。
- 美国指数 9 月 10 日 Yahoo 更新仍有来源失败，独立于本次 A 股 9 月 11 日补跑。

核验入口：在服务器 `server` 目录执行 `node scripts/repairSeptember11.mjs audit`，读取数据库与已发布清单。

本次仅将该服务器恢复为可自动采集状态，未执行迁移或样本初始化脚本。

## 新闻与龙虎榜补充修复（21:05）

按用户要求，将服务器 `DRAGON_TIGER_ENABLED`、`MARKET_NEWS_ENABLED` 从 `false` 改为 `true`，备份 `.env` 后重启后端。新闻按现有默认每 3 分钟采集，龙虎榜每日 18:00 采集、18:30 复查，启动时自动补采。

- 9 月 11 日龙虎榜已入库 68 条，启动补采及两个调度时段任务均成功。
- 首轮新闻采集返回 71 条，东方财富、财联社、新闻联播三个来源均成功。
- 使用管理台同一健康检测逻辑核验：龙虎榜新鲜度、新闻心跳、新闻来源成功率均为 `pass`，新闻心跳与抓取距今约 1 分钟。
- 核验命令：服务器 `server` 目录执行 `node --import tsx scripts/repairSeptember11.mjs audit-collectors`。

## 市场健康与宏观调度（21:14）

按用户要求，持久化设置 `MARKET_HEALTH_ENABLED=true`，明确保留 `MARKET_OPINION_PUSH_ENABLED=false`，并重启后端加载。Python 使用服务器虚拟环境 `/opt/quant-backtest/.venv/bin/python`。

- 每个自然日 10:30 检查宏观 PPI 数据、更新名义盈利周期；20:30 更新基本面、估值与市场结构评分。重启后检查当天到期任务并补跑。
- 首轮宏观任务成功读取 248 条观测，当前可用周期为 2026-07，没有新增观测。
- 后端仍为开机自启；邮件推送调度保持关闭。
