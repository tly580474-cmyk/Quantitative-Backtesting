# Ubuntu 全量副本验收记录

验收完成：2026-09-09 22:32（北京时间）。目标 `192.168.2.218`，Ubuntu 26.04.1，6 核、约 7.1 GiB 内存。程序位于 `/opt/quant-backtest`。

**结论：全量复制、Linux 适配和运行验收通过，未切换主服务器。** Windows 后端健康正常，原研究快照、分钟行情、资金流等更新任务保持原状态。目标机不承担自动更新、因子自动调度或模拟交易调度。

## 数据完整性

| 项目 | 结果 |
| --- | --- |
| MySQL | 89 张表，51,023,065 条记录，逐表精确行数校验通过 |
| 证券目录 | 5,919 条，与源库一致，接口返回正常 |
| 日线研究快照 | 17,302,974 条；发布快照校验通过 |
| 分钟行情 | 2010—2026 年，4,053 个 Parquet，3,510,970,453 条记录；文件数及字节数与 manifest 一致 |
| 数据和历史产物 | 41,213 个文件，85,597,228,610 字节（约 79.719 GiB）；全部内容哈希校验通过 |
| 程序源码 | 发布包中 1,085 个文件与安装目录逐文件一致 |
| 演示视频 | `Intro.mp4` 单独复制，两端 SHA-256 一致 |
| 数据库文件引用 | 189 项路径完成 Linux 映射；源端存在的文件在目标全部存在 |

数据文件统计包含研究快照、分钟库、报告、附件、实验输出、缓存、日志、研究备份和本项目智能体会话副本；不包含重新安装的 Linux 依赖及 MySQL 导入后的存储文件。

数据库由 MySQL 9.6 以 InnoDB 一致性事务导出，导入 MySQL 8.4。导出时间为 21:34:30—21:38:18，压缩备份大小 1,810,003,394 字节，SHA-256：

```text
d7a4f0ada1c84d881a4cbf71d5108ebf684def39e70d93dee3636c309e814a80
```

87 张表导出前后行数相同，目标精确匹配。源机持续更新的两张表单独核对：采集运行记录 13,990→13,991，目标 13,990；新闻 4,201→4,209，目标 4,201。副本代表此次备份及文件复制的时间范围，不是此后持续同步的镜像。

## 运行验收

- 前台、管理台构建成功；后端类型检查通过；Linux 调度回归 5 项通过。
- 因子与快照回归 2 项在 Windows、Ubuntu 两端均通过。
- 使用 2026-09-09 真实行情物化简单价格因子，5,550 个标的全部生成有效值；产物位于独立验收目录。
- 分钟 Parquet 抽样读取 100 条通过；未运行全市场重新采集或新的全市场研究任务。
- Codex、Claude 均通过新会话真实模型调用和迁移会话续聊。Codex 沙箱内实际命令成功返回 `Python 3.14.4`。
- 页面认证、全量证券目录、日线接口、管理 API 权限、快照接口、历史报告下载、智能体历史列表及全部附件内容哈希，共 8 项应用冒烟检查通过。
- 从 Windows 实际访问 8080 前台、8081 管理台及管理 API，均通过。
- 前台开关关闭期间管理入口保持可用，随后恢复；管理 API 触发后端重启后自动恢复，进程 PID 已更新。
- 重启后再次确认后台总开关关闭、四个更新 timer 不活动。API 仅监听 `127.0.0.1:3001`，MySQL 仅监听 `127.0.0.1:3306`。

## 本次修复与配置

1. 新增 `BACKGROUND_JOBS_ENABLED` 总开关；默认不改变原机行为，副本设置为 `false`。安装脚本据此保持四个更新 timer 停用，MySQL event scheduler 关闭。
2. 附件、报告及因子产物的 Windows 路径仅在目标库转换；原始数据库备份保留。
3. Codex 的 36 条原会话及三个 SQLite 状态库转为同版本 Linux 原生 schema，保留全部业务记录并逐表比较。解决跨平台 SQLx 校验值差异，未修改校验值来绕过验证。
4. Claude 使用源机当前 Qwen 上游，移除对 Windows CC Switch 回环代理的依赖；本项目历史会话完整复制。应用独立固定 CLI 版本，Claude 自动更新关闭。
5. 修复全量财务快照下价格因子投影缺少市值列的问题；统一关联日期精度，兼容两端 pandas/Arrow 环境。
6. 修复传输清单对硬链接的哈希计算，校验实际文件后保留完整原始传输清单。程序和智能体工作目录归服务账户 `quant` 所有。

## 已存在的源端缺失

以下 4 个数据库引用在迁移前就没有对应文件，在本项目备份中也未找到。已保留记录并记入验收证据，未生成替代内容：

- 智能体报告：`9a0f9aa9-e516-442e-8082-4abc652f38f2.html`。
- 实验缓存：`e195b8653bb52ba19ebd06966c41a1a66be9e482378fe42bc1d42764fce6fe02.pdf`。
- 实验缓存：`a37822e7812ccf62437b791ef3e05dc968c24ea00315644a82a2773862e66a4c.pdf`。
- 实验缓存：`ebac7a778ef7a74219ff339784bd4e8614445b6bae04e0763ed2eed870a900a0.html`。

## 访问与证据

- 前台：<http://192.168.2.218:8080>。
- 管理台：<http://192.168.2.218:8081>。
- 网页账号及管理令牌：目标 `/root/quant-staging-access.txt`，权限 0600。
- 原始备份、全部传输清单、数据库与文件校验、路径映射及运行验收 JSON/日志：目标 `/var/lib/quant-migration`。
- 本地备份和验收材料：仓库 `tmp_output/linux-migration-20260909`。
- 部署方式及再次同步注意事项：[全量副本部署说明](FULL-DATA-STAGING.md)。

正式切换前仍需同步原机在此次备份之后的增量，并另行交接自动更新任务。此次没有调整原机域名、FRP 或主服务器职责。
