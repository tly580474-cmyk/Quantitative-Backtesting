# 智能体报告图片超限故障调查

服务器：`192.168.2.218:/opt/quant-backtest`。

## 原任务与复现

- 对话/原任务：`25a42897-2dbe-4cdd-aea2-f0d209872fea`。
- 请求：对全 A 股市场展开宏观盈利质量分析，不包括金融和石油行业。
- Provider：Pi；原任务结束于 2026-09-22 21:19:05（北京时间），模型进程退出码 0。
- 最终正文和研究产物均存在；失败发生在后台将最终 Markdown 转为静态报告时。
- 正文声称 12 张图，实际引用 13 个不同 PNG 路径，其中拆分子图分别计数。
- `collectReportAssets` 在读取图片前触发 12 张上限；`createStaticReport` 原先
  捕获所有异常并仅返回 false，导致页面统一显示 `REPORT_INVALID_OR_MISSING`。
- 对原文仅移除第 13 个图片引用进行隔离复现，12 张图形成的 HTML 为
  1,579,639 字节，完整静态安全校验通过。该复现未发布为用户报告。

## 修复

保留图片数量、目录隔离、类型、体积和 HTML 安全限制。
为受控图片校验异常增加明确类型，将实际图片数及操作建议保存到任务错误信息和终态事件。
HTML 校验同样保留具体原因；未知内部异常仅在服务器日志记录，对页面返回通用错误，
避免暴露内部细节。系统提示补充按最终 Markdown 的不同图片路径计数，以及续写时
把复用图片复制到本轮目录的要求。

本地和服务器均通过 17 项相关测试及 TypeScript 类型检查，包括 12 张通过、13 张拒绝、
超限原因进入任务记录和终态事件，以及既有主动内容、越界、SVG 等保护。

## 重生成

通过原对话消息接口发起后续轮次，沿用 Pi 会话：
`3378c931-a50d-4673-b99c-18eccfc1325c`。

要求复用已有研究证据、整理至不超过 12 张图、在本轮目录保存最终 Markdown 并使用
同一套报告函数做完整预检，然后由平台正式保存。另要求复核原文额外排除 ST 股票
与原始用户范围不一致的问题。

服务器备份与调查记录：`/opt/quant-report-fix-20260922/`；
原始正文备份：`server/.cache/report-investigation/original.md`。

## 交付验收

续写任务在 2026-09-22 22:02:18（北京时间）完成，状态为 `completed`，
报告记录 ID 为 65，包含 12 张内嵌图片；报告已经纳入 ST 公司并重新核对相关统计。
正式文件保留智能体最终答复中研究标题之后的完整正文，移除了开头的调试预检说明，
原任务事件记录保持原样。预览和下载接口均通过静态校验。

智能体续写中的调试序言曾误写旧报告有 14 张图；该数字不作为调查结论。
本次直接读取原始 `assistant_final`，按实际 Markdown 引用及 `collectReportAssets`
复现确认是 13 张；正式报告已移除这段调试序言。

最终验收机器记录：`/opt/quant-report-fix-20260922/acceptance.json`。
