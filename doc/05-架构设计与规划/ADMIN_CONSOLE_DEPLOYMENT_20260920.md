# 管理台改造部署验收（2026-09-20）

已部署至 `192.168.2.218:/opt/quant-backtest`。用户明确批准切换现有服务后执行，完成时间约北京时间 22:22；22:23 的部署后检查通过。

管理台入口：<http://192.168.2.218:8081>。

## 发布结果

- 更新 24 个源码、测试和配置示例文件，以及管理台构建资源；生产额外修补未覆盖。
- `quant-backend` 已恢复，PID 从 `157821` 变为 `188058`，MySQL 健康。
- 3001 直连与 8081 Nginx 下的 health、overview、config、agent、data-update-progress、database-backup 共 12 项请求全部返回 200。
- Claude 版本为 `2.1.142 (Claude Code)`，默认 Provider 仍为 Claude。
- `server/.env` 发布前后 SHA-256 一致；四个原本活跃的系统定时器已恢复。
- 管理台静态入口与隔离浏览器验收的构建完全一致，Nginx 服务账户可读。旧哈希资源保留，避免已打开页面立即缺失资源。
- 回滚备份位于 `/opt/quant-backtest/.deploy-backups/20260920-admin-hardening`，包含原源码、原管理台静态资源及文件哈希清单。

## 验收范围与证据

| 项目 | 结果 |
| --- | --- |
| Linux 管理台构建、服务端类型检查 | 通过 |
| Linux 专项回归 | 81 项通过；修正了 Node 22 下 SQL 测试的运行环境声明 |
| 配置编辑 | 真实浏览器验证普通值回填、保存后列表刷新；测试配置在隔离目录中修改并恢复 |
| 局部故障 | 公网访问接口故意返回错误时，总览和其他功能继续可用 |
| Claude 与业务密钥 | Claude 卡片可见；密钥不回填；带前后缀的管理令牌被清除 |
| MySQL 连接恢复 | 只锁定临时账户并断开其连接，管理接口呈现 healthy → critical → healthy；未停止生产 MySQL |
| 鉴权限流 | 隔离 HTTP 服务前 9 次失败返回 401，第 10、11 次返回 429，变化的转发头无法绕过 |
| 真实 SQL 导出与恢复 | 临时库经 mysqldump 导出，下载哈希一致，恢复库验证两条测试记录正确；保留策略只留下 3 份 |
| 外部删除备份 | 删除隔离目录的最新测试备份后，状态转为失败；重新导出成功 |
| 1 GiB 浏览器下载 | 合成文件原生下载完成，两端 SHA-256 相同；采样到的页面 JS 堆约 7.1 MiB，无页面异常，未通过 fetch/XHR 加载文件内容 |
| 真实 MySQL 执行计划 | 外层使用 `idx_sj_status_created`，恢复判定使用 `idx_sj_run_key` |
| 进度缓存 | 线上 5 个并发请求返回同一个采集时间戳；缓存命中请求约 2 ms |
| 入口保护 | 8080/8081 页面未认证返回 401；管理 API 缺少 Bearer 返回 401 |
| 清理 | 临时数据库、测试账户、测试服务、SSH 隧道和两端合成大文件已清理 |

下载测试 SHA-256：`49bc20df15e412a64472421e13fe86ff1c5165e18b2afccf160d4dc19fe68a14`。

结构化证据：[admin_hardening_deployment_acceptance_20260920.json](admin_hardening_deployment_acceptance_20260920.json)。本地浏览器截图保存在 `tmp_output/deploy-admin-hardening/browser-overview.png`；远端隔离验收结果保存在 `/opt/quant-admin-hardening-20260920/acceptance-state`。

## 当前诊断与边界

线上总览为 warning：1/8 个数据域未通过覆盖检查，8 个研究结果引用旧快照。本次没有改动市场数据、重新发布快照或清理这些研究结果。

生产库约 8.9 GB，本次没有执行其全量导出或恢复。真实导出与恢复使用临时库，大文件测试使用独立合成文件；测试证明下载机制和基本备份流程，不代表全量生产恢复演练。

生产页面 Basic 登录未完成复测：服务器保存的网关口令与现有认证配置不匹配。未更改认证配置。浏览器交互测试在回环隔离服务上使用与生产完全相同的构建；生产 Nginx 的鉴权边界、API 转发、文件可读性及资源哈希已验证。

生产数据库中仍保留一条自 2026-08-14 起标记为 running 的历史同步记录（`f40ef437-e2bd-4510-a5ab-c38cb00b3255`）。发布前没有活跃 Agent 或系统更新服务；本次没有修改该历史记录。
