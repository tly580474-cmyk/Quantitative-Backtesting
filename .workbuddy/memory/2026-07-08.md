# 2026-07-08

## 任务：抓取 factors.directory 技术指标因子清单
- 来源站点 https://factors.directory/zh ，技术指标分类共 **89 个因子**（取自 sitemap.xml）。
- 页面为 Next.js (RSC)，无分类列表页（/zh/factors 404）；每个详情页只列"相关因子"。全量 URL 从 sitemap.xml 的 /en/factors/tech/<slug> 提取。
- 详情页公式数据藏在 RSC 负载里，是**双重 JSON 转义**（`\\`→`\` 需 json.loads 两次）；符号含义用非贪婪+前瞻 `\\"(?=[,}\]])` 提取，避免贪婪吞掉整段。
- 交付物（工作区根目录）：
  - `tech_factors_table.html` — 可查看总表（搜索 + KaTeX 渲染 + 符号含义折叠）
  - `tech_factors.csv` — 结构化（index,zh,en,slug,latex,symbol_meanings,meaning,url），UTF-8-BOM 供 Excel
  - `tech_factors.json` — 结构化原始数据
  - `parse_tech.py`（89 页 HTML→抽取）/ `generate_html.py`（JSON→HTML/CSV）
- 数据存于 C:/Users/qjmzc/AppData/Local/Temp/techpages/*.html（bash 的 /tmp）。
- 下一步：可基于该清单做单因子/组合 IC 回测验证（用户长期在做中证2000因子挖掘）。

## 任务：筛选"支持日K数据"的技术因子
- 用户要求从 89 个技术因子中筛出可用日K（OHLCV）数据计算的因子。
- 判定方法：`tech_factors.json`(符号含义+释义) → `analyze_dailyk.py` 关键词分类 + 符号级人工核验。中文一词多义导致多次误判，最终规则：
  - 以**符号含义（变量定义）**为主信号，释义仅用无歧义强词（分钟/高频/逐笔/盘口/大单/主力资金等）；排除"交易日内/资金流指标/$\epsilon$(eps)/分时图"等套话。
  - 额外新增非日K数据源类别：分析师目标价、技术关联度矩阵、基金/外资持仓、新闻/另类。
- 结果（共 89）：**支持日K 60 个**（纯日K 51 + 需日频额外字段 9：换手率1、市值1、指数/市场7）；**不支持 29 个**（高频/L2 23、基本面2、持仓2、分析师1、技术关联1）。
- 易错点（已逐条符号核验修正）：ADL/CMF/IMI/TRIX/MACD/CCI/BIAS 等经典指标被"结合基本面/交易日内"套话误伤 → 实际纯日K；amplitude-cutting-momentum 的"日内振幅"=(H-L)/C 属日K；turnover-uniformity(分钟级)、adjust-expected-yield(分析师)、technology-momentum(技术关联)、fund/foreign-broker-network(持仓) 确为非日K。
- 交付物：`tech_factors_dailyk.html`（可筛选+徽章+KaTeX）、`tech_factors_dailyk.csv`（全量含 data_requirement/supported 列）、`tech_factors_supported.csv`（60 个支持组）、`generate_dailyk.py`。
- 后续：支持组可对接本地 `932000_中证2000_全部数据.xlsx` 跑 IC/ICIR。

## 任务：公式正确渲染 + 符号含义补全（tech_factors_dailyk.html）
- **符号含义补全**：旧解析只认 `meaning` 键，但站点对符号含义字段名**有的是 `meaning`、有的是 `explanation`、有的是 `description`**（不一致）。已扩展正则同时匹配三者。
- 关键坑：`formulaExplanation` 内除每个符号的 `explanation` 外，还有 **2 条整段级简介**也被 `(?:meaning|explanation)` 抓入，导致含义数组比符号数组多、整体错位。修正：只在 `symbols:[...]` 子数组内提取含义。
- `vertical-horizontal-filter`(VHF) 站点**压根无 `symbols` 数组**（只有 text 散文），已从该因子自身 latex（HCP/LCP/A/B/VHF/N）手动补充符号含义，标记知识来源。
- 结果：89 个因子符号含义 **0 缺失、0 空值**（之前 15 个空 + VHF 全缺）。
- **公式渲染修正**：改用**构建期 KaTeX 预渲染**（render_katex.js + 本地 katex.min.js）把所有公式+符号名渲染成静态 HTML 内嵌，移除浏览器端 auto-render/CDN JS（仅保留 KaTeX CSS），离线也能正确显示。273 公式 + 326 符号渲染 **0 失败**。
- 渲染期预处理：未转义的 `%` 会被 LaTeX 当注释吃掉——修了 `information-dispersion` 的 `[%neg - %pos]`；符号名首尾/中间的 `$` 定界符（如 `$$P_{i,t}$`）统一剥除；中文混 math 模式用 strict:false 消告警（仍正确渲染）。
- 交付管线（可复现）：`parse_tech.py`(抽取, OUT→工作区 tech_factors.json) → `render_katex.js`(Node 预渲染→tech_factors_katex.json) → `generate_dailyk.py`(HTML/CSV)。
- 注意：katex.min.js 在 C:/Users/qjmzc/.workbuddy/binaries/node/workspace/ ；89 页原始 HTML 在 C:/Users/qjmzc/AppData/Local/Temp/techpages/ 与 refetch/(16 个重抓)。

## 清理：删除本任务全部中间产物（13:52）
- 用户要求只保留最终 HTML。已删除工作区 11 个中间文件：tech_factors_table.html、tech_factors.csv、tech_factors.json、parse_tech.py、generate_html.py、tech_factors_dailyk.json、analyze_dailyk.py、generate_dailyk.py、tech_factors_supported.csv、tech_factors_dailyk.csv、tech_factors_katex.json。
- 工作区仅保留 `tech_factors_dailyk.html`（1.8MB，公式已预渲染、符号含义已展开，可独立打开无需任何依赖）。
- 一并清理：node 工作区 katex.min.js / render_katex.js / test_katex.js；Temp 下 techpages/(89页) 与 refetch/(16页) 抓取源。
- 结论：HTML 自包含，删除脚本/数据不影响查看。若日后需重新生成，需重新抓取+重跑管线（脚本已删）。
