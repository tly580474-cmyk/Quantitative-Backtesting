# 2026-07-07 工作日志

## 图表详情面板：右上角 → 左上角（限定范围）
- 需求：将「行情分析」和「策略回测」界面的图表详情面板从右上角移到左上角。
- 范围边界（用户明确要求）：只动行情分析/策略回测侧，**严禁改动市场界面**。
- 改动文件：
  - `src/features/chart/CandleDetail.tsx`：详情面板本体，prop 由 `right` 改为 `left`，inline style 定位 `right` → `left`。
  - `src/features/chart/ChartContainer.tsx`：调用处由 `<CandleDetail right={showChipProfile && period === 'day' ? 182 : 8} />` 改为 `<CandleDetail left={8} />`，去掉原给右侧筹码峰（ChipProfile，宽 ~170px，居图表右侧）的让位逻辑——移到左侧后不再冲突。
- 未动：`src/index.css`、市场界面 `src/features/marketData/MarketKlineChart.tsx`（其 tooltip 由 `.market-chart-tooltip` 类控制）。
- 验证：`npx tsc -b` 通过（exit 0）。
- 背景：CandleDetail 仅被 ChartContainer 引用；ChartContainer 由 App.tsx（行情分析）和 BacktestRunner.tsx（策略回测）共用，与市场界面（MarketKlineChart）完全独立。
